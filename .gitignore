# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
dist/
build/

# Virtual environments
.venv/
venv/

# Env / secrets
.env
.env.*
!.env.example

# Test / lint / type caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
.coverage.*
coverage.xml

# uv
uv.lock.bak

# Editors / OS
.vscode/
.idea/
*.swp
.DS_Store

# Never let PHI data-repo paths get nested/committed here by accident
/data/

# Claude Code agent worktrees (local only)
.claude/

# External review documents kept locally as working input, not project record
# (gemini-review.md is also markdown that `ruff format --check .` scans for
# python code blocks, and its blocks are not ruff-formatted — it broke CI once
# after a `git add -A` swept it in).
gemini-review.md
